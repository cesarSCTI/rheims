feat: popular categories section

New sections/popular-categories.liquid + blocks/_popular-category-card.liquid:
title (editable text/tag/color from section settings), a CTA button (label,
link, font, background/text/hover colors), and a responsive card grid where
each card links to a Collection block-by-block, with a per-card image
override (falls back to the collection featured image, then a placeholder
with an "Imagen no disponible" caption when neither is set).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
